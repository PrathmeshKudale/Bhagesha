# Kisan Doctor (Plantix Clone)

A mobile-first farming assistant for Indian farmers.

### Features
- 📸 **Crop Doctor:** Upload leaf photos to detect diseases (Mock AI).
- 🌦 **Weather:** Real-time local weather without API keys.
- 💬 **Community:** Farmer-to-farmer Q&A forum.
- 📱 **UI:** Designed like a native mobile app (Green/Clean).

### How to Run
1. Install requirements: `pip install -r requirements.txt`
2. Run the app: `python app.py`
3. Open `http://localhost:5000`
